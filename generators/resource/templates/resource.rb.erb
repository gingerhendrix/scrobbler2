module Scrobbler2
  class <%= name.classify %> < Base
    
    def initialize()
      @query = {}#TODO eg. {:artist => artist, :track => track}
    end
    <% funcs.each do |func| %>
     get_resource :<%=func%>, :root => "<%=func%>"
   <% end %>
  end
end
